[pytest]
# src/ on sys.path so tests can `import server` and `import telemetry` directly
pythonpath = src
testpaths = tests
# Show short traceback, verbose test names
addopts = -v --tb=short
log_cli = true
log_cli_level = WARNING